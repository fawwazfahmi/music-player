# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Brainstorming visual companion artifacts (mockups, server state)
.superpowers/

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# YouTube cookie jars — live Google credentials, never commit.
# Root-anchored so it can't swallow source dirs like src/app/api/yt-cookies.
/yt-cookies/
*.cookies.txt
/cookies.txt

# Phase 6 audio analysis: pre-trained model weights (venv lives outside the repo — see scripts/audio/setup.sh)
models/audio/
scripts/.title-cleanup.json
scripts/.title-cleanup-snapshot.json
